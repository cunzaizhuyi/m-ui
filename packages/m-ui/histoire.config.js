import { defineConfig } from 'histoire'

export default defineConfig({
  // your Histoire configuration
  // setupFile: 'index.js'
  tree: {
    groups: [
      {
        id: 'comp',
        title: 'components'
      },
    ]
  }
})